# Jumpit v1.9 release receipt

Released 2026-08-26 from the verified `v1.8.0` kid-directed campaign.

## Hidden Lights

- Five authored Hidden Lights sit on reachable side paths, exactly one in each place.
  Normal deterministic runs finish all twenty bells without collecting one; five separate
  discovery replays each find their regional light and still finish with stable hashes.
- Discovery is optional and immediate. A warm bloom, original three-note cue, and short
  haptic acknowledge it without pausing controls. Found lights stay lit through respawn,
  restart, reload, campaign runs, and daily or friend challenges.
- Fresh Home shows no empty secret checklist. After the first discovery it reveals one
  compact five-stamp strip; Trails marks only places whose light was found. Normal clear
  sheets stay unchanged, and neither partial nor complete discovery gates progression.
- The campaign ending remains available at zero lights. Its copy and five small lantern
  marks reflect partial or complete discovery without adding another task or reward economy.

## Mobile art and installed-shell continuity

- Seven canvas PNGs totaling 15,800,394 bytes were converted into 2,104,068 bytes of
  deterministic WebP art, an 86.7% reduction. First play assigns only the three Garden
  images; changing place tears down the old set before assigning the next one.
- Every generated source, root launcher icon, style reference, and derived WebP has a
  closed file path, exact dimensions, license, SHA-256, and reproducible transform record.
- Real Chromium and WebKit pages require `image/webp`, await every image decode, verify
  exact natural dimensions, and read sprite pixels to prove transparency survived.
- The exact shipped v1.8 worker and updater are immutable fixtures. A controlled v1.8
  client upgrades once into the sole coherent v1.9 cache; a failed future precache cannot
  mutate that active shell.

## Verification

- Logic, runtime, replay, PWA, install, update, worker, asset, copy, save, audio, and
  migration contracts: 86/86.
- Source phone suite: 68/68 across Chromium at 420x912 and WebKit at 390x844.
- Rebuilt production phone suite: 68/68 on the same matrix.
- Deterministic shared-runtime replay runner: all 20 authored trails pass. Five additional
  Hidden Light replays each discover exactly one light and finish with identical hashes.
- Canonical house PWA checker against the exact 28-file, 3,578,328-byte build: 26/26.
- Theme-token lint, AA contrast across all four looks, public-diff architecture lint,
  closed provenance, and three independent cold P1/P2 reviews: clean.
- In-app browser inspection at 420x912 and 390x844: fresh Home, live play, pause sheet,
  controls, and bottom navigation fit without page scroll, clipping, or console errors.
- Physical-iPhone offline/update verification and the cold-kid comprehension gate remain
  v2 human-device gates; this receipt does not claim them.
